// pub trait ShortestPath {
//     fn is_preprocessed(&self);
//     fn preprocess(&mut self);
//     fn compute_shortest_path(&mut self, src: usize, dst: usize) -> f64;
// }

use super::Graph;
pub mod dijkstra;
